'use client'
import { useState, useMemo } from 'react'
import Link from 'next/link'
import { Search, SlidersHorizontal, X, Bookmark, MapPin, Clock, ChevronRight } from 'lucide-react'
import PageHeader from '@/components/layout/PageHeader'

const SECTOR_ACCENT: Record<string, string> = { law_firm: '#1A1A1A', banking: '#1A1A1A', energy: '#7A3B00', fintech: '#0E5C3A', other: '#3B3B3B' }
const SECTOR_LABELS: Record<string, string> = { law_firm: 'Law Firm', banking: 'Banking', energy: 'Energy', fintech: 'Tech & Fintech', other: 'Industry' }
const TYPE_LABELS: Record<string, string> = { job: 'Full-time', internship: 'Internship', vacation_scheme: 'Vacation Scheme', pupillage: 'Pupillage' }
const SECTOR_OPTIONS = [{ value: '', label: 'All Sectors' }, { value: 'law_firm', label: 'Law Firms' }, { value: 'banking', label: 'Banking & Finance' }, { value: 'energy', label: 'Energy & Extractives' }, { value: 'fintech', label: 'Technology & Fintech' }, { value: 'other', label: 'Multinationals & Other' }]
const TYPE_OPTIONS = [{ value: '', label: 'All Types' }, { value: 'job', label: 'Full-time' }, { value: 'internship', label: 'Internship' }, { value: 'vacation_scheme', label: 'Vacation Scheme' }, { value: 'pupillage', label: 'Pupillage' }]
const LEVEL_OPTIONS = [{ value: '', label: 'All Levels' }, { value: 'student', label: 'Student' }, { value: 'nysc', label: 'NYSC' }, { value: 'junior', label: 'Junior (0-3 yrs)' }, { value: 'mid', label: 'Mid (3-6 yrs)' }, { value: 'senior', label: 'Senior (6+ yrs)' }]
const LOCATION_OPTIONS = [{ value: '', label: 'All Locations' }, { value: 'Lagos', label: 'Lagos' }, { value: 'Abuja', label: 'Abuja' }, { value: 'Port Harcourt', label: 'Port Harcourt' }]

export default function JobsClient({ jobs }: { jobs: any[] }) {
  const [search, setSearch] = useState('')
  const [sector, setSector] = useState('')
  const [type, setType] = useState('')
  const [level, setLevel] = useState('')
  const [location, setLocation] = useState('')
  const [showFilters, setShowFilters] = useState(false)
  const [saved, setSaved] = useState<Set<string>>(new Set())
  const filtered = useMemo(() => jobs.filter(l => { if (search && !l.title.toLowerCase().includes(search.toLowerCase()) && !l.employer.toLowerCase().includes(search.toLowerCase())) return false; if (sector && l.sector !== sector) return false; if (type && l.type !== type) return false; if (level && l.level !== level) return false; if (location && !l.location?.toLowerCase().includes(location.toLowerCase())) return false; return true }), [jobs, search, sector, type, level, location])
  const activeChips = [sector && { key: 'sector', label: SECTOR_OPTIONS.find(o => o.value === sector)?.label || sector }, type && { key: 'type', label: TYPE_OPTIONS.find(o => o.value === type)?.label || type }, level && { key: 'level', label: LEVEL_OPTIONS.find(o => o.value === level)?.label || level }, location && { key: 'location', label: location }].filter(Boolean) as { key: string; label: string }[]
  const clearFilter = (key: string) => { if (key === 'sector') setSector(''); if (key === 'type') setType(''); if (key === 'level') setLevel(''); if (key === 'location') setLocation('') }
  const toggleSave = (id: string) => { setSaved(prev => { const next = new Set(prev); next.has(id) ? next.delete(id) : next.add(id); return next }) }
  return (
    <main style={{ backgroundColor: '#FAF7F2', paddingTop: '80px', minHeight: '100vh' }}>
      <PageHeader        heading="legal opportunities"
        subcopy="Roles across law firms, banks, energy companies, fintechs, NGOs, and regulators. For all career stages."
      >
          <div style={{ display: 'flex', gap: '0.75rem', flexWrap: 'wrap' as const }}>
            <div style={{ position: 'relative' as const, flex: '1 1 280px' }}>
              <Search size={14} style={{ position: 'absolute' as const, left: '12px', top: '50%', transform: 'translateY(-50%)', color: '#4A4A4A' }} />
              <input type='text' placeholder='Search roles, employers...' value={search} onChange={e => setSearch(e.target.value)} style={{ width: '100%', padding: '0.6rem 1rem 0.6rem 2.2rem', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.85rem', color: '#1A1A1A', backgroundColor: '#FAF7F2', border: '0.5px solid #E8E0D5', borderRadius: '2px', outline: 'none', boxSizing: 'border-box' as const }} />
            </div>
            <button onClick={() => setShowFilters(!showFilters)} style={{ display: 'flex', alignItems: 'center', gap: '6px', padding: '0.6rem 1.25rem', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.75rem', fontWeight: 600, letterSpacing: '0.08em', textTransform: 'uppercase' as const, backgroundColor: showFilters ? '#1A1A1A' : '#FAF7F2', color: showFilters ? '#FAF7F2' : '#1A1A1A', border: '0.5px solid #E8E0D5', borderRadius: '999px', cursor: 'pointer' }}>
              <SlidersHorizontal size={13} />Filters{activeChips.length > 0 ? ' (' + activeChips.length + ')' : ''}
            </button>
          </div>
          {showFilters && <div style={{ marginTop: '1rem', padding: '1.25rem 1.5rem', backgroundColor: '#FAF7F2', border: '0.5px solid #E8E0D5', borderRadius: '2px', display: 'flex', gap: '1.5rem', flexWrap: 'wrap' as const }}>{[{ label: 'Sector', value: sector, setter: setSector, options: SECTOR_OPTIONS }, { label: 'Type', value: type, setter: setType, options: TYPE_OPTIONS }, { label: 'Level', value: level, setter: setLevel, options: LEVEL_OPTIONS }, { label: 'Location', value: location, setter: setLocation, options: LOCATION_OPTIONS }].map(({ label, value, setter, options }) => <div key={label} style={{ display: 'flex', flexDirection: 'column' as const, gap: '5px' }}><label style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 700, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: '#4A4A4A' }}>{label}</label><select value={value} onChange={e => setter(e.target.value)} style={{ padding: '0.4rem 0.75rem', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.82rem', color: '#1A1A1A', backgroundColor: '#FAF7F2', border: '0.5px solid #E8E0D5', borderRadius: '999px', cursor: 'pointer', minWidth: '150px' }}>{options.map(o => <option key={o.value} value={o.value}>{o.label}</option>)}</select></div>)}</div>}
          {activeChips.length > 0 && <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap' as const, marginTop: '0.75rem', alignItems: 'center' }}>{activeChips.map(chip => <button key={chip.key} onClick={() => clearFilter(chip.key)} style={{ display: 'flex', alignItems: 'center', gap: '5px', padding: '3px 10px', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.72rem', color: '#1A1A1A', backgroundColor: '#EEE8DF', border: 'none', borderRadius: '999px', cursor: 'pointer' }}>{chip.label} <X size={10} /></button>)}<button onClick={() => { setSector(''); setType(''); setLevel(''); setLocation('') }} style={{ padding: '3px 6px', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.72rem', color: '#000000', background: 'none', border: 'none', cursor: 'pointer' }}>Clear all</button></div>}
      </PageHeader>
      <div style={{ maxWidth: 'min(1800px, 94vw)', margin: '0 auto', padding: '1.5rem 2rem 3rem' }}>
        {filtered.length === 0 ? <div style={{ textAlign: 'center' as const, padding: '5rem 0' }}><p style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '1.2rem', color: '#1A1A1A', marginBottom: '0.5rem' }}>No listings match your filters.</p><p style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.85rem', color: '#4A4A4A' }}>Try broadening your search.</p></div> : <div style={{ display: 'flex', flexDirection: 'column' as const, gap: '1px', backgroundColor: '#E8E0D5', border: '0.5px solid #E8E0D5' }}>{filtered.map(listing => { const accent = SECTOR_ACCENT[listing.sector] || '#3B3B3B'; const isSaved = saved.has(listing.id); return <article key={listing.id} style={{ backgroundColor: '#FFFFFF', borderLeft: '3px solid ' + accent, padding: '1.25rem 1.5rem', display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: '1rem' }}><div style={{ flex: 1, minWidth: 0 }}><div style={{ display: 'flex', gap: '0.75rem', alignItems: 'center', marginBottom: '0.4rem', flexWrap: 'wrap' as const }}><span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 700, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: accent }}>{SECTOR_LABELS[listing.sector] || 'Industry'}</span>{listing.tier && <span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: '#4A4A4A' }}>{listing.tier}</span>}<span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: '#4A4A4A' }}>{TYPE_LABELS[listing.type] || listing.type}</span>{listing.is_verified && <span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 700, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: '#2D6A4F' }}>Verified</span>}{listing.is_closing_soon && <span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.6rem', fontWeight: 700, letterSpacing: '0.1em', textTransform: 'uppercase' as const, color: '#000000' }}>Closing Soon</span>}</div><Link href={'/jobs/' + listing.slug} style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '1rem', fontWeight: 600, color: '#1A1A1A', textDecoration: 'none', display: 'block', lineHeight: 1.3, marginBottom: '0.2rem' }}>{listing.title}</Link><p style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.8rem', color: '#1A1A1A', marginBottom: '0.6rem', fontWeight: 500 }}>{listing.employer}</p><div style={{ display: 'flex', gap: '1.25rem', alignItems: 'center', flexWrap: 'wrap' as const }}><span style={{ display: 'flex', alignItems: 'center', gap: '3px', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.72rem', color: '#4A4A4A' }}><MapPin size={10} />{listing.location}</span>{listing.is_rolling ? <span style={{ fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.68rem', fontWeight: 700, letterSpacing: '0.08em', textTransform: 'uppercase' as const, color: '#4A4A4A' }}>Rolling</span> : listing.deadline ? <span style={{ display: 'flex', alignItems: 'center', gap: '3px', fontFamily: 'Schibsted Grotesk, sans-serif', fontSize: '0.72rem', color: listing.is_closing_soon ? '#000000' : '#4A4A4A' }}><Clock size={10} />{new Date(listing.deadline).toLocaleDateString('en-NG', { day: 'numeric', month: 'short', year: 'numeric' })}</span> : null}</div></div><div style={{ display: 'flex', alignItems: 'center', gap: '1rem', flexShrink: 0 }}><button onClick={() => toggleSave(listing.id)} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: '4px', color: isSaved ? '#1A1A1A' : '#C0B8B0', display: 'flex' }}><Bookmark size={15} fill={isSaved ? '#1A1A1A' : 'none'} /></button><Link href={'/jobs/' + listing.slug} style={{ color: '#1A1A1A', display: 'flex', alignItems: 'center' }}><ChevronRight size={18} /></Link></div></article>})}</div>}
      </div>
    </main>
  )
}
