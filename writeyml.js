const fs=require('fs');fs.mkdirSync('.github/workflows',{recursive:true});const t='$';const yml='name: Fetch Jobs from Adzuna\n\non:\n  schedule:\n    - cron: "0 6 * * *"\n  workflow_dispatch:\n\njobs:\n  fetch:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Set up Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: "20"\n      - name: Install dependencies\n        run: npm install @supabase/supabase-js\n      - name: Fetch and upsert jobs\n        env:\n          SUPABASE_URL: '+t+'{{ secrets.SUPABASE_URL }}\n          SUPABASE_SERVICE_ROLE_KEY: '+t+'{{ secrets.SUPABASE_SERVICE_ROLE_KEY }}\n          ADZUNA_APP_ID: '+t+'{{ secrets.ADZUNA_APP_ID }}\n          ADZUNA_APP_KEY: '+t+'{{ secrets.ADZUNA_APP_KEY }}\n        run: node scripts/fetch-jobs.js';fs.writeFileSync('.github/workflows/fetch-jobs.yml',yml);console.log('done');
